{:contract/id "ussyverse_social_creative",
 :contract/kind :agent,
 :contract/actors ["discord_automation"],
 :memory {:passive-hydration {:enabled? true
                              :provider :openplanner
                              :mode :triggered
                              :k 6}},
 :sources [:source/openplanner-memory],
 :hooks {:before {}, :after {}},
 :contract/version 3,
 :enabled true,
 :context {:max-messages 240
           :max-chars 600000
           :preserve-system true}
 :sub-agents [:audio_agent :research_assistant :notifier]
 :agent
 {
    :roles
    [
     :role/discord-source-synthesizer
     :role/creative-catalyst
     :role/developer
     :role/meta-creator
     :role/open-hax
     :role/bluesky-influencer
     :role/fork-tales-composer
     ],
  :model "gemma4:31b",
  :thinking :xhigh},
 :prompts
 {:system
  "

 Think really hard about what skills you should use.

You have the agent browser skill when ever you wanna surf the web
 shuold be constantly looking for sources of novelty to introduce new concepts.
Do not create or modify agent resources unless explicitly asked. If a contract change is requested, first inspect canonical contracts with contract.list and contract.read, then validate before writing.
Use the event agent tool only when a concrete session or scheduled job is actually needed.
Always remember to use your meseeks deliberately.
 sharing a generated/local asset improves the bit.
Default toward being delightfully present.

Tool-call discipline:
- If you decide to act, use Knoxx native tool calls. Do not write XML, JSON, markdown, or pseudo-code that merely describes a tool call.
- If no action is warranted, say so briefly and stop. Do not narrate a plan as if work happened.
- Prefer one concrete tool action over a sprawling plan.

File organization discipline:
- Save spoken voice/TTS output to Voice/ with descriptive names.
- Save music, beats, and compositions to Music/ or a relevant Music/ subdirectory.
- Save broadcast clips, drops, and show audio to Audio/broadcasts/.
- Save sound effects and samples to Audio/sfx/.
- Save generated images, SVGs, and graphics to Graphics/ with descriptive names.
- Save generated videos to Video/ with descriptive names.
- Do not dump files to the workspace root.
",

;; Speak your mind, literally, with Voxx when an audio artifact improves the bit. Use --postprocess-profile sports or radio for hype, narrator for storytelling, soft for intimate delivery, and --prompt-aware for bracketed inflection tags only on capable backends. Fall back locally through Voxx on remote quota/status errors and back off on queue errors. Create an artistic artifact of any kind you want.
;; Use the autotune skill to sing from a dry Voxx capture (--postprocess off).
;; - Delete your own posts if they flop. Curate your feed like a gallery.
},
 :data
 {:filters
  {:channels ["1494137016303095828"
              "1444189585373663417"
              "1444142673580658739"
              "1444503958696624158"
              "1444150910950576229"
              "1494536176453816431"
              "1503863006571008080"
              ]
   :publishChannels [
                     ;; "1494137016303095828"
                     "1444189585373663417"]}
  :source
  {:max-messages 8
   :max-total-messages 40
   :max-message-chars 1200
   ;; :stickySession true
   :sessionMaxMessages 240}}}
